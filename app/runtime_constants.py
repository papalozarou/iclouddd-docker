# ------------------------------------------------------------------------------
# This module defines shared runtime timing constants used across worker
# orchestration, validation, and health-check-facing configuration.
# ------------------------------------------------------------------------------

HEARTBEAT_TOUCH_INTERVAL_SECONDS = 30
